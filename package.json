{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "serve": "serve dist",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix", 
    "format": "prettier --write ."
  },
  "dependencies": {},
  "devDependencies": {}
}